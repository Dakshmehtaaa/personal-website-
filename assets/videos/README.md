Expected here (referenced by why-sustainability-matters.html):

- `for-companies.mp4` — the looping background video in the video section between
  the hero and "Where the advantage actually comes from". H.264 MP4, ideally
  under a few MB — it autoplays for every visitor on page load.
- `for-companies-poster.jpg` — a still frame shown before the video loads/plays
  (also what shows if autoplay is blocked). Optional but recommended.
